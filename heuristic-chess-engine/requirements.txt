# Chess Engine — Web Application Dependencies
# ============================================
# Install all dependencies with:
#   pip install -r requirements.txt

# ── Chess engine ──────────────────────────────────────────────────────────
# python-chess / chess: move generation, FEN parsing, Zobrist hashing.
chess>=1.10

# ── Web server / API bridge ───────────────────────────────────────────────
# Flask: lightweight WSGI web framework that serves the frontend and
# acts as middleware between the browser and the UCI engine process.
flask>=3.0

# Optional: flask-cors adds CORS headers if you ever serve the frontend
# from a different origin (e.g. a separate dev server).
# flask-cors>=4.0
